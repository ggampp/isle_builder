/** Tamanho de um tile em unidades de mundo (1 unidade = 1px lógico a zoom 1). */
export const TILE_SIZE = 16;

/** Tiles por lado de um chunk (chunks são a unidade de re-mesh/culling). */
export const CHUNK_SIZE = 32;
